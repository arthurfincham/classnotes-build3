language: ruby
cache: bundler
dist: xenial
rvm: 2.7.3
env:
 - DB=pgsql
services:
  - postgresql
script:
 — bundle install — jobs=3 — retry=3
 — bundle exec rake db:create
 — bundle exec rake db:migrate
 — bundle exec rspec